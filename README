# Fancy History

Fancy History is a Python script that generates fake commits throughout a specified year to fill your GitHub contribution graph.

## Requirements

-   Python 3.x
-   Git

## Configuration

Edit `src/fancyGitHistory.py` to set:

-   **TARGET_YEAR**: The year for commit generation (e.g., `2021`)
-   **BRANCH_NAME**: The target branch (e.g., `main`)
-   **REMOTE_URL**: Your remote repository URL
-   **MIN_COMMITS_PER_DAY** & **MAX_COMMITS_PER_DAY**: Commit range per day
    The commit log is stored in `src/commit_log.txt`.

## Usage

1. Open a terminal and navigate to your project root:
    ```bash
    cd /path/to/your/project
    ```
2. Ensure your repository is clean (commit or stash any changes).
3. Run the script:
    ```bash
    python3 src/fancyGitHistory.py
    ```
    You will see a color-coded progress bar and status messages.
4. When finished, all commits will be pushed to your remote.
